rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to all documents during initial development.
    // In a production app, these should be restricted to authenticated users or specific owners.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}