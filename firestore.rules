
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // UPDATED: Rules re-deployed to ensure write permissions are active for studio-3673070449-f277c
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
