
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // FORCE REDEPLOY: Updated at 2024-05-21T10:00:00Z to ensure wide-open permissions for development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
