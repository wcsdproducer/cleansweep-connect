
{
  "entities": {
    "User": {
      "title": "User",
      "description": "A platform user with an assigned role.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["Customer", "Admin", "Service Provider"] },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "role"]
    },
    "ServiceProvider": {
      "title": "Service Provider",
      "description": "Details about a service provider applying to join CleanSweep.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "city": { "type": "string" },
        "experience": { "type": "string" },
        "teamSize": { "type": "string" },
        "expertise": { "type": "array", "items": { "type": "string" } },
        "status": { "type": "string", "enum": ["pending", "approved", "rejected"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["firstName", "lastName", "email"]
    },
    "ProviderCoverage": {
      "title": "Provider Coverage",
      "description": "Geospatial coverage settings for a service provider.",
      "type": "object",
      "properties": {
        "providerId": { "type": "string" },
        "center": {
          "type": "object",
          "properties": {
            "lat": { "type": "number" },
            "lng": { "type": "number" }
          }
        },
        "radiusMiles": { "type": "number" },
        "selectedZips": { "type": "array", "items": { "type": "string" } },
        "excludedZips": { "type": "array", "items": { "type": "string" } },
        "geohash": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    },
    "Job": {
      "title": "Job",
      "description": "A scheduled cleaning service job.",
      "type": "object",
      "properties": {
        "clientName": { "type": "string" },
        "type": { "type": "string" },
        "time": { "type": "string" },
        "address": { "type": "string" },
        "status": { "type": "string", "enum": ["Confirmed", "Pending", "Completed"] },
        "price": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "Message": {
      "title": "Message",
      "description": "A message in a conversation thread.",
      "type": "object",
      "properties": {
        "sender": { "type": "string", "enum": ["client", "admin", "provider"] },
        "text": { "type": "string" },
        "time": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    },
    "Invoice": {
      "title": "Invoice",
      "description": "A payment record for a completed job.",
      "type": "object",
      "properties": {
        "invoiceId": { "type": "string" },
        "date": { "type": "string" },
        "amount": { "type": "string" },
        "status": { "type": "string", "enum": ["Paid", "Processing", "Pending"] },
        "client": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password", "google.com"]
  },
  "firestore": {
    "users/{userId}": {
      "schema": "User",
      "description": "General user accounts and roles."
    },
    "serviceProviders/{providerId}": {
      "schema": "ServiceProvider",
      "description": "Registration records for cleaning service providers."
    },
    "provider_coverage/{providerId}": {
      "schema": "ProviderCoverage",
      "description": "Geospatial coverage settings."
    },
    "jobs/{jobId}": {
      "schema": "Job",
      "description": "Scheduled cleaning jobs."
    },
    "messages/{messageId}": {
      "schema": "Message",
      "description": "Chat messages."
    },
    "invoices/{invoiceId}": {
      "schema": "Invoice",
      "description": "Billing records."
    }
  }
}
